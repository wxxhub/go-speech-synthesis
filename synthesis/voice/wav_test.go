package voice

import (
	"testing"
)

func TestGenConst(t *testing.T) {
	//file, _ := os.Create("const.txt")
	//defer file.Close()
	//fileMap, _ := os.Create("map.txt")
	//defer fileMap.Close()
	//
	//data := ""
	//mapData := ""
	//
	//keys := make([]string, 0)
	//for k := range dataMap {
	//	keys = append(keys, k)
	//}
	//
	//sort.Slice(keys, func(i, j int) bool {
	//	if keys[i] < keys[j] {
	//		return true
	//	}
	//
	//	return false
	//})
	//
	//for _, k := range keys {
	//	name := strings.ToTitle(k)
	//	data += fmt.Sprintf("VOICE_%s VOICE = \"%s\"\n", name, k)
	//	mapData += fmt.Sprintf("VOICE_%s: %s,\n", name, k)
	//}
	//
	//file.Write([]byte(data))
	//fileMap.Write([]byte(mapData))
}
